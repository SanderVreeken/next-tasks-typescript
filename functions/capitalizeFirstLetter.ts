// Function that return the input string with the first letter capatalized.
const capitalizeFirstLetter = (string: string) => string.charAt(0).toUpperCase() + string.slice(1)

export default capitalizeFirstLetter